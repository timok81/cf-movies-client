//UNUSED COMPONENT

// import { Row, Col } from "react-bootstrap";
// import { MovieCard } from "../movie-card/movie-card";

// export const FavMovies = ({ movies, onFavToggle, favouriteMovies }) => {
//   let favMovies = movies.filter(movie => favouriteMovies.includes(movie.id))

//   return (
//     <Row className="g-4">
//       {favMovies.map((movie, index) => (
//         <Col className="mb-4" key={index} md={3}>
//           <MovieCard
//             movie={movie}
//             isFav={favouriteMovies.includes(movie.id)}
//             onFavToggle={onFavToggle}
//           />
//         </Col>
//       ))}
//     </Row>
//   );
// };
